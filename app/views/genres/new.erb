<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <ul>
    <% if @error %>
      <% @errors.each do |error| %>
        <li><%= error %></li>
      <% end %>
    <% end %>
    </ul>
    <h1>Sorry, we never heard of that Genre</h1>
    <h2>Double check, but you might want to create it</h2>

    <%= form_for @genre do |f| %>
      <%= f.label :genre_name %> <br>
      <%= f.text_field :genre_name %> <br>
      <%= f.label :description %> <br>
      <%= f.text_field :description %><br>
      <%= f.submit "Make the Genre" %>

    <% end %>

  </body>
</html>
